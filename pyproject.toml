[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "vecscaledb"
version = "0.1.0"
requires-python = ">=3.11"
description = "VecScaleDB — Distributed Vector Database"

[tool.pytest.ini_options]
asyncio_mode = "auto"
